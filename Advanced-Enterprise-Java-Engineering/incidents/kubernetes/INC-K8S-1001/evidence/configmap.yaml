# ConfigMap excerpt — baypay-prod / payment-config
# Gate: 3
# Teaching equivalent: kubectl get configmap payment-config -n baypay-prod -o yaml
# Synthetic BayPay. Captured 2026-11-03T18:26:00Z after the 17:40 UTC roll.

apiVersion: v1
kind: ConfigMap
metadata:
  name: payment-config
  namespace: baypay-prod
  labels:
    app: payment-service
  annotations:
    baypay.example/ticket: BAYPAY-10011
    baypay.example/last-applied-by: sam.okada
data:
  SPRING_PROFILES_ACTIVE: prod
  BAYPAY_DB_USER: baypay_app
  BAYPAY_LOG_LEVEL: INFO
  JAVA_TOOL_OPTIONS: -XX:+UseContainerSupport -XX:MaxRAMPercentage=75

# data keys present: SPRING_PROFILES_ACTIVE, BAYPAY_DB_USER, BAYPAY_LOG_LEVEL, JAVA_TOOL_OPTIONS
# kubectl describe shows Environment Variables from: payment-config
# CLUSTER.md lists BAYPAY_DB_* from env. Compare keys in this excerpt to that contract.
# resourceVersion: "1844201"
