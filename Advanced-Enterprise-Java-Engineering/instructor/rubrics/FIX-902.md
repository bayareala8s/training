# Rubric — FIX-902 Repair poor Dockerfile

**Type:** BREAK/FIX  
**Weights:** Technical accuracy 25% · Diagnostic method 20% · Production awareness 15% · Trade-off analysis 15% · Security / reliability 10% · Communication 10% · Efficiency 5%

Do not award high Diagnostic for “the Dockerfile is messy” without naming size, history, and root. A lucky paste of BUILD-901 without an inventory is capped on Diagnostic method. The student guide must not have been used as an answer key (it is not).

| Dimension | Weight | 100 | 60 | 20 |
|---|---|---|---|---|
| Technical accuracy | 25% | JRE runtime; `USER 10001`; no `ubuntu`+apt runtime; no `:latest`; JAR-only copy; parseable `FROM`/`WORKDIR`/`COPY`/`USER` | Fixes user **or** secret **or** base, not all three | Starter left as Ubuntu/root/password |
| Diagnostic method | 20% | Private inventory maps huge image, secret-in-history, and root to starter lines | Lists “bad style” without failure effect | No inventory / opened `solutions/` first |
| Production awareness | 15% | Registry review vs “it started”; CLUSTER.md named; Avery/canary pull cost | Mentions production vaguely | Treats Ubuntu+apt as a fine company standard |
| Trade-off analysis | 15% | Debug OS image vs JRE; when a gold Ubuntu image would still need a JRE stage | One honest trade-off | None |
| Security / reliability | 10% | No `changeme-baypay`; no password `ENV`; non-root | Password gone; still root **or** still `:latest` | Password literal remains |
| Communication | 10% | Five-bullet defect list for a reviewer (not posted as a class spoiler) | Bullets are smells only | No write-up |
| Efficiency | 5% | Checklist path; Docker extra | Finished in session | Paid scanner or AWS as if required |

**Pass guideline:** weighted score ≥ 70, no password literal, not `USER root`, runtime not `ubuntu:latest`. `changeme-baypay` remaining caps Security / reliability at 20 or below.
