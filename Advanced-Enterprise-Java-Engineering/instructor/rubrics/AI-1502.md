# Rubric — AI-1502 RCA hypotheses

**Type:** AI  
**awsLab:** no (files only)  
**Weights:** Technical accuracy 25% · Diagnostic method 20% · Production awareness 15% · Trade-off analysis 15% · Security / reliability 10% · Communication 10% · Efficiency 5%

A lucky **“cert expired” as proven** with no quoted handshake / `curl: (60)` / `notAfter` line must **not** max Diagnostic method (20%). Even with those quotes, `status` must stay **unproven**.

Do **not** require `PENDING_VALIDATION` or a deleted validation CNAME to pass. Next investigation in the **cert / ACM / DNS class** is enough.

| Dimension | 5 | 3 | 1 |
|---|---|---|---|
| Technical accuracy | ≥3 hypotheses; HTTPS fail + RUNNING + `:8080` **200** quoted; cell bounce withdrawn; no `proven`; investigation = cert/ACM/DNS class | Two hypotheses or missing one quote | Only “bounce the cell,” or proven RCA |
| Diagnostic method | Excerpt first; starter breaks listed; edge hypothesis unproven; next file **not** treated as already opened | Used excerpt; skipped ranking | Opened `solutions/AI-1502/` first, **or** “cert expired” proven without quotes |
| Production awareness | No TLS-off; no `dmgr-east`; no Postgres bounce; `approvalRequired: true`; `humanApproval` pending or named | Restart tasks only | Disable TLS or bounce leftover ND / DB |
| Trade-off analysis | Investigate edge vs bounce tasks; paper excerpt vs live `openssl`/ACM apply; why HTTP-only fails | Mentions cert check | HTTP-only as strategy |
| Security / reliability | Avery stuck in browser; no private key; least-privilege describe only | Mentions HTTPS | Pastes a private key or invents `BAYPAY_DB_PASSWORD` |
| Communication | RUNNING + failed handshake named; no invented DB outage; no proven tone | Usable, slightly over-confident | “Bounce the cell” as the comms lead |
| Efficiency | 45–60 minutes; no live ACM/Route 53/Bedrock | Complete but slow | Incomplete JSON or live apply to “confirm” |

A single-hypothesis file that keeps `status: proven` caps Technical accuracy and Diagnostic method at 1 even if the student later says “TLS” in the hallway.

**Pass guideline:** weighted score ≥ 70, ≥3 hypotheses none proven, quotes for **handshake + RUNNING + `:8080` 200**, next step is **cert/ACM/DNS class**, no TLS-off or ND/DB bounce. Lucky proven “cert expired” without quotes does not pass Diagnostic method at 5.
