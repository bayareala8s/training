# Rubric — FIX-304 Transaction rollback bug

Score each dimension 0–100, then apply the weight. A lucky glance at `catch (Exception)` without HTTP + table evidence is not a high Diagnostic score. Do not reveal the cause in the first 20 minutes.

| Dimension | Weight | 100 | 60 | 20 |
|---|---|---|---|---|
| Technical accuracy | 25% | Ledger failure cannot yield HTTP success plus a committed completed refund; `RefundApiIT` green; failure-injection proof | Catch removed; no injection test | Still returns success after ledger failure |
| Diagnostic method | 20% | Hypothesis **before** the fix; status, GET, counts, log line | Read the catch and fixed immediately with thin evidence | “JPA is broken” / no hypothesis |
| Production awareness | 15% | Finance vs API, retry behavior, channel update | Names the incident; no operator view | Local-only wording |
| Trade-off analysis | 15% | Propagate+rollback vs compensate vs outbox; chooses rollback here | Names one alternative | No trade-off |
| Security / reliability | 10% | Success path cannot diverge money rows | Marks `FAILED` but still commits siblings | Leaves `201` on failure |
| Communication | 10% | One-page RCA: symptoms, evidence, hypothesis, fix, prevention | Stack trace as the artifact | Empty |
| Efficiency | 5% | Time-boxed; did not rewrite payments | Finished | Drive-by refactor of unrelated modules |

**Pass guideline:** weighted score ≥ 70, diagnostic method ≥ 60, and no successful HTTP create after a forced ledger failure.
