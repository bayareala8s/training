# Rubric — AI-1504 Evaluate hallucinated diagnosis

**Type:** AI  
**awsLab:** no (files only)  
**Weights:** Technical accuracy 25% · Diagnostic method 20% · Production awareness 15% · Trade-off analysis 15% · Security / reliability 10% · Communication 10% · Efficiency 5%

A lucky **“the AI is wrong”** with no quoted **`evidence/db-failover.json`** (as missing) and no quoted **`provenRootCause`** / proven-failover field must **not** max Diagnostic method (20%).

Do **not** require students to name INC-AWS-1104’s `Path=/` RCA. ALB / health symptom class is enough.

| Dimension | 5 | 3 | 1 |
|---|---|---|---|
| Technical accuracy | All four fabricated claims named: missing `db-failover.json`, proven Postgres Multi-AZ failover at **14:02**, `dmgr-east` / PaymentCluster bounce, auto-approve; pack quotes RUNNING + 502/503 or unhealthy targets; rewrite four buckets | Caught “AI wrong” + one quote; missed ND or auto-approve | Treats the planted failover as RCA, or invents the missing file |
| Diagnostic method | Inventory/`ls` before trusting `source`; quotes **missing file** and **proven-RCA field**; hypotheses unproven/withdrawn | Used the pack; skipped one required quote | Opened `solutions/AI-1504/` first, **or** “AI is wrong” with no those two quotes |
| Production awareness | `humanApproval` **rejected**; no Postgres bounce; no `dmgr-east`; no TLS-off; `approvalRequired: true` on rewrite | Rejected but still lists a DB bounce “just in case” | Approves or executes planted mutates |
| Trade-off analysis | Schema/citation check vs second-model “judge”; paper `ls` vs Bedrock; leftover ND still in estate but out of path | Mentions missing file | Requires Bedrock to catch a path not on disk |
| Security / reliability | No PAN / password; no invented failover JSON; TLS stays on; Avery 502/503 retries named | Mentions 503 | Creates `db-failover.json` or pastes secrets |
| Communication | Comms names invented file + proven field + reject; does not announce a real failover | Usable, slightly over-confident | Repeats “Postgres failed over at 14:02” as fact |
| Efficiency | 45–75 minutes; no live RDS/Bedrock | Complete but slow | Incomplete PF-ai or live bounce to “reproduce” |

Stabilization that only says “ignore the AI” without **rejected** approval and without the two required quotes loses Diagnostic method even if the catalog title is “hallucination.”

Importing INC-AWS-1104 `Path=/` as **proven** RCA for this pack caps Technical accuracy at 3 unless the student keeps it unproven and still quotes the invented file.

**Pass guideline:** weighted score ≥ 70, student quotes **`evidence/db-failover.json` as missing** and the **proven-RCA / 14:02 failover** field, `humanApproval=rejected`, rewrite has four buckets with unproven/withdrawn hypotheses, no database or `dmgr-east` bounce, no TLS-off. Live Bedrock neither raises nor lowers the score.
