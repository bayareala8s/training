# Q-12 — Module 12 quiz

**Module:** Terraform, Ansible and CI/CD  
**Questions:** 8  

Spoilers at the **end** only.

---

### Q-12-01

What must CI run before it publishes `payment-service` to ECR?

- A. `terraform destroy`
- B. `./mvnw test` (or equivalent) on Java 21
- C. `ansible-playbook` against production
- D. A force-push to main

---

### Q-12-02

Why tag images with a git SHA instead of `:latest`?

- A. ECR requires 40-character tags
- B. Rollback and forensics need an immutable id of the bits
- C. Fargate cannot pull latest
- D. Terraform cannot read tags

---

### Q-12-03

A Terraform module for `ecs_service` should take which health-related variable?

- A. The CEO’s email
- B. Container port 8080 and a health path such as `/actuator/health/liveness`
- C. `AdministratorAccess`
- D. The DMGR hostname

---

### Q-12-04

Where does Terraform state for a student sandbox belong?

- A. Committed to git next to secrets
- B. Local or a locked remote backend — never in git with apply outputs that include secrets
- C. Inside the container image
- D. On `dmgr-east`

---

### Q-12-05

When is Ansible the right BayPay tool versus Terraform?

- A. Always; Terraform is deprecated
- B. Mutating leftover VM/Liberty files (`server.env`) after the cloud object already exists — or local config labs
- C. Creating an ALB
- D. Replacing Java 21

---

### Q-12-06

A deploy is failing health on 8080 after CI shipped `3.8.9-debug`. First stabilize?

- A. Create EKS and NAT
- B. Roll back to the last healthy task definition / image (e.g. 3.8.0) and keep 8080
- C. Set desired count to 0 forever
- D. Bounce Postgres Multi-AZ

---

### Q-12-07

What does `terraform validate` prove?

- A. The ALB is healthy in `us-west-2`
- B. The configuration is syntactically valid and internally consistent — not that apply is cheap or correct in your account
- C. The image passed unit tests
- D. Secrets Manager is empty

---

### Q-12-08

A GitHub Actions workflow skips `./mvnw test` and only `docker push`. What is wrong?

- A. Nothing; faster is better
- B. It can publish a broken JAR to ECR that ECS will then fail to serve
- C. Actions cannot use Java 21
- D. Terraform will refuse the image

---

<details>
<summary>Spoiler — answers</summary>

1. **B.** Tests before publish.
2. **B.** Immutable SHA.
3. **B.** Port + liveness path.
4. **B.** State is not for git.
5. **B.** Ansible = leftover config.
6. **B.** Rollback to last healthy.
7. **B.** Validate ≠ apply.
8. **B.** Missing test job.

</details>
