# Q-13 — Module 13 quiz

**Module:** Production Engineering and Observability  
**Questions:** 8  

Spoilers at the **end** only.

---

### Q-13-01

Which three signals are the RED method for `POST /api/v1/payments`?

- A. Replication, encryption, and durability
- B. Rate, errors, and duration (including a tail such as P99)
- C. Restarts, events, and deployments
- D. Region, AZ, and task count

---

### Q-13-02

BayPay’s Module 13 availability SLO for payment create is which number, and what is excluded from burn by default?

- A. 99.99%; exclude all 5xx
- B. 99.9%; exclude typical 4xx (except a cohort may treat 429 as capacity)
- C. 99%; include every 4xx as downtime
- D. 100%; any log ERROR burns the budget

---

### Q-13-03

Which Micrometer labels are acceptable on a payment-create timer?

- A. `customerId`, `accountId`, and `Idempotency-Key`
- B. `uri`, `method`, `outcome`, and coarse `status`
- C. PAN last four and Avery Chen’s UUID
- D. The raw `paymentId` on every success

---

### Q-13-04

Priya pages on which class of signal first?

- A. CPU greater than 80% on any task
- B. Any log line that contains ERROR
- C. SLO burn (fast and slow windows) and saturation that predicts burn
- D. A dashboard panel that failed to load

---

### Q-13-05

A JSON access log for a COMPLETED payment should include which field set?

- A. PAN, CVV, and `BAYPAY_DB_PASSWORD`
- B. `correlationId`, `paymentId`, `outcome` — never PAN
- C. The full `Idempotency-Key` as a Prometheus label
- D. Avery Chen’s government id

---

### Q-13-06

Throughput falls and P99 rises after a release. What is a Staff first move?

- A. Bounce `dmgr-east` and Postgres Multi-AZ
- B. Quote RED + a resource/scrape panel, write a hypothesis, then open the next gated file — do not max diagnostic on “database” with no quote
- C. Set `-Xmx` equal to the container memory limit
- D. Declare 99.99% and add a region

---

### Q-13-07

Capacity planning in Module 13 is primarily about which headroom?

- A. A second region’s idle EKS control plane
- B. RPS, heap, Hikari pending, and servlet threads against the 99.9% / 400 ms P99 contract
- C. How many `PaymentCluster` members Morgan Hale can start
- D. NAT Gateway throughput

---

### Q-13-08

Jordan wants to merge a metrics change to `main` at 17:50 with no review. What is the change-management answer?

- A. Force-push so history looks clean
- B. Protected `main`, conventional review, and a named rollback — a green pipeline is not a merchant-healthy cut
- C. Skip CI because dashboards are “just JSON”
- D. Apply EKS so the change is more visible

---

<details>
<summary>Spoiler — answers</summary>

1. **B.** RED = rate, errors, duration.
2. **B.** 99.9% SLO; typical 4xx excluded.
3. **B.** Low-cardinality labels only.
4. **C.** Page on burn and leading saturation.
5. **B.** Structured ids, never PAN.
6. **B.** Quotes and gates, not a lucky “database.”
7. **B.** Operational headroom vs the SLO.
8. **B.** Change control is still git + review + rollback.

</details>
