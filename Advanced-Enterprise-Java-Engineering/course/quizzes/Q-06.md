# Q-06 — Module 6 quiz

**Module:** WebSphere Liberty Modernization  
**Questions:** 8  
**Case study:** BayPay Financial Services (fictional, synthetic data)

Choose one answer for each question. Answers are in the spoiler at the **end** only.

---

### Q-06-01

What is the best one-line contrast between `BayPayCell` and a Liberty payment server?

- A. Liberty cannot do JDBC; ND cannot do HTTP
- B. ND is a managed cell (DMGR, node agents, synchronized nodes); Liberty is a process configured by `server.xml` and features you enable
- C. Liberty requires a deployment manager; ND does not
- D. They are identical operations models with different logos

---

### Q-06-02

A Liberty `server.xml` for `payment-service.war` omits `jdbc-4.3` and `jndi-1.0` but includes `servlet-6.0`. What fails first?

- A. The JVM cannot start at all
- B. HTTP works; lookup of `jdbc/baypay-payment` fails because the JDBC/JNDI features were not enabled
- C. SIBus `BayPayBus` starts automatically anyway
- D. LTPA tokens are minted without a user registry

---

### Q-06-03

On Liberty, why should payment use `jdbc/baypay-payment` instead of the historical cell-wide `jdbc/baypay`?

- A. Liberty forbids the string `baypay`
- B. So payment and refund (and reporting) do not share one pool and one outage
- C. JNDI names cannot contain a hyphen
- D. The Spring Boot reference app cannot run if any JNDI name exists

---

### Q-06-04

A compatibility assessment finds `payment.ear` still compiled against `javax.servlet` and a SIBus-only MDB. What is the honest classification?

- A. Lift-and-shift this weekend; Liberty is a drop-in ND
- B. Package-rename may be mechanical; the SIBus MDB is rewrite or defer — not a silent lift
- C. Delete the ear and tell finance to use spreadsheets
- D. Keep ND forever because jakarta packages are illegal

---

### Q-06-05

Which `server.xml` fragment is acceptable for BayPay production secrets?

- A. `<properties.postgresql password="changeme-baypay"/>`
- B. `<properties.postgresql password="${env.BAYPAY_DB_PASSWORD}"/>` plus the value in `server.env` / a secret store, not in git
- C. Put the password in `featureManager`
- D. Hard-code the J2C alias `baypayDbAlias` inside the WAR

---

### Q-06-06

Why is Wave 1 on BayPay refund, not payment?

- A. RefundCluster has no database
- B. Refund is lower volume and a smaller blast radius; payment stays on ND until the Liberty path is proven
- C. Liberty cannot host a payment WAR
- D. IHS cannot route to more than one cluster

---

### Q-06-07

Wave 2 puts one Liberty payment replica behind `ihs-east`. Liberty p99 is 4× ND and error rate climbs. What is the rollback?

- A. Install a new traditional ND cell in AWS
- B. Drain the canary; send 100% of payment traffic back to PaymentCluster; keep the Liberty config for a fix
- C. Stop `Pay1`, `Pay2`, and `Pay3` so Liberty is forced to win
- D. Raise `jdbc/baypay` `maxConnections` on the cell to 500

---

### Q-06-08

When is Spring Boot the better BayPay target than a Liberty WAR this quarter?

- A. Never; this course forbids Boot after Module 3
- B. New modules and the teaching reference app; Liberty when an ear must remain an ear on a short timeline
- C. Only when you need SIBus
- D. Only when you need a DMGR

---

<details>
<summary>Spoiler — answers and explanations</summary>

1. **B.** ND is a managed cell; Liberty is feature + `server.xml`.
2. **B.** Features are gated. Servlet ≠ DataSource.
3. **B.** Isolated pools; do not copy the cell-wide bind.
4. **B.** `javax` may be toolable; SIBus is not a silent lift.
5. **B.** Password from env / secret store, not git.
6. **B.** Smaller blast radius first.
7. **B.** Rollback is traffic back to `PaymentCluster`.
8. **B.** Boot for new/teaching; Liberty for ear-compatible waves.

</details>
