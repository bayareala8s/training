# Q-11 — Module 11 quiz

**Module:** AWS Container Platforms  
**Region:** us-west-2  
**Questions:** 8  

Spoilers at the **end** only.

---

### Q-11-01

Why is `:latest` a bad ECR tag for `payment-service` in `us-west-2`?

- A. ECR forbids the word latest
- B. You cannot prove which bits a task ran, and two deploys may pull different images
- C. Fargate cannot pull tagged images
- D. Avery Chen’s UUID cannot be in a tag

---

### Q-11-02

Why is ECS on Fargate the student-apply default instead of EKS?

- A. EKS cannot run Java
- B. EKS control plane and often NAT/nodes cost more than a 90-minute lab needs; Fargate is enough to learn task defs and ALB
- C. OpenShift forbids Fargate
- D. Fargate includes RDS for free

---

### Q-11-03

ECS task RUNNING, ALB target unhealthy, `GET /` returns 404 from Spring. First honest class?

- A. The cluster is out of EC2
- B. The load balancer health check does not match a 200 the app actually serves
- C. Route 53 is down worldwide
- D. `dmgr-east` must be bounced

---

### Q-11-04

Who should read Secrets Manager for `BAYPAY_DB_PASSWORD`?

- A. The ALB
- B. The task role (app) or execution role (inject at start) — never a plaintext env in git
- C. Anonymous internet
- D. The ECR scan role

---

### Q-11-05

An idle ALB left over the weekend is a problem because:

- A. ALB is free after 1 hour
- B. ALB hourly + LCU charges continue with zero traffic; destroy-after-lab exists for this
- C. Fargate pays the ALB bill
- D. CloudWatch cannot see ALBs

---

### Q-11-06

When might EKS or OpenShift beat ECS for BayPay?

- A. Never; this course forbids them
- B. When you already run Kubernetes/OpenShift skills, need portable YAML, or platform features ECS does not provide — and you accept the cost
- C. When you want NAT Gateways for fun
- D. When you must run WebSphere ND

---

### Q-11-07

Student apply in this course should create RDS Multi-AZ because payments need a database. True?

- A. True; always create prod RDS in a 90-minute lab
- B. False; RDS is literacy in L-11.7; local H2/Postgres or a disclosed estimate — do not spawn Multi-AZ for the lab
- C. True if the region is `us-east-1`
- D. False because Spring cannot use RDS

---

### Q-11-08

Can you pass BUILD-1101 without `terraform apply`?

- A. No; AWS is pass/fail only if the ALB is green
- B. Yes; paper + `terraform validate` + a destroy plan is the designed path if you cannot spend
- C. Only if you use EKS
- D. Only on OpenShift

---

<details>
<summary>Spoiler — answers</summary>

1. **B.** Pin SHA or version.
2. **B.** Fargate default; EKS costs more.
3. **B.** Health path/port, not “ECS dead.”
4. **B.** Roles + Secrets Manager, not git.
5. **B.** Idle ALB still bills.
6. **B.** Trade-off, not a slogan.
7. **B.** No Multi-AZ RDS in the 90-minute apply.
8. **B.** Validate is enough.

</details>
