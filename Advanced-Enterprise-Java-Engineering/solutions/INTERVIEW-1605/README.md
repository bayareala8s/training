# INTERVIEW-1605 — Instructor solution

**Do not share this file with students before a loop log has three timestamps from one sitting.**

This folder is a **sequence checklist**, not a key of spoken answers. Score the **mode switch**. Do not require a portal, Bedrock, or AWS apply.

Required sequence (student lab): **practice/timed + (rapid fire *or* troubleshooting) + design slice**.

## Slot 1 — practice / timed (must)

- ≥2 bank ids, **two** maturity voices each (not clones).
- ≥1 item on an **8-minute** clock (`timed-interview` or student timer).
- **No** `--reveal` until both drafts exist.
- Avery or `c1605e55-0000-4000-8000-111111111605` named once in the sitting.

Reuse of last week’s 1601 notes **without** a new timed item today does **not** satisfy the slot.

## Slot 2 — exactly one mid-mode (must)

**Either** `python3 interview-bank/simulator.py --mode rapid-fire --count 10` with short answers  
**or** INTERVIEW-1603 method on HTTPS+RUNNING **or** P99+rate.

If troubleshooting: Gate 1 quotes + unproven hypotheses + next evidence class. Lucky RCA **must not** max Diagnostic method. Do **not** lecture 1301/1402/1104/1205 instructor RCAs.

Doing **both** mid-modes is extra, not required. Doing **neither** fails the loop.

## Slot 3 — design slice (must)

[PF-design.md](../../student/worksheets/PF-design.md) has a this-quarter decision (99.99% create **or** monolith vs extract), a drawing, ≥3 trade-offs, refusals.

If INTERVIEW-1604 already completed the page, a **dated loop addendum** (8-minute spoken cut + what they refused to reopen) is enough **only if** the underlying decision is still present.

Slogan-only slice fails Technical accuracy the same way 1604 does.

## Mode-switch note (must)

Student explains what they **shortened** leaving practice, what they **refused to whiteboard** in slot 2, and how the design slice stayed a **decision**. Missing note caps Communication.

## Sitting integrity

- Start and end timestamps, same calendar day, one block (break ≤10 minutes).
- One memorized paragraph reused in every slot caps Technical accuracy.
- Reveal during slot 1 caps Diagnostic method.
- Apply / Bedrock / portal-required caps Production awareness.

## Comms (acceptable example)

Full mock, Phase A. Timed one JVM item at eight minutes, then ten rapid-fire landings (or one HTTPS symptom-class method). Design slice: multi-AZ single-region for Avery’s create; operated SLO still 99.9%. No portal, no apply, leftover ND not in path. Bank still 100.
