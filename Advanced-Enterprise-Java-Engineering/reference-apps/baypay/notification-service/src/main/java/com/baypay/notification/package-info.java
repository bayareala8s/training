/**
 * Completion notifications. Separate from {@code Payment} on purpose (SOLID S).
 * This records a synthetic email/webhook row; it does not talk to a real ESP.
 */
package com.baypay.notification;
