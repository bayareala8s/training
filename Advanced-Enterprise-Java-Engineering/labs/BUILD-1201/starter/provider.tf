# BUILD-1201 starter — intentionally incomplete.
# Add terraform.required_providers, bind the provider to var.region (us-west-2),
# and finish variables.tf / ecr.tf / outputs.tf.
# Do not put access keys in this file.

provider "aws" {
  region = "us-east-1"
}
