# Course player

Serve from the **repository root** (not this folder):

```bash
./scripts/start_course.sh
```

Open http://localhost:8080/course-ui/

Progress is stored in the browser (`localStorage` key `baylearn-eia-progress-v1`).
