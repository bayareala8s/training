# Local lab defaults (copy to .env if you use a dotenv helper; Terraform uses terraform.tfvars)

# Default region for AWS labs and capstones
AWS_REGION=us-west-2

# Never put access keys here. Use `aws configure` or an IAM role in your sandbox.
